export const HTMLInputPasswordError = {
  typeError: '[Input Error] Password',
  msg: 'Password is required and must have more than six characters, one number, one uppercase and one lowercase letter'
}

export const HTMLInputPasswordConfirmError = {
  typeError: '[Input Error] Password Confirm',
  msg: 'Password not valid'
}
export const HTMLInputPasswordComparetionError = {
  typeError: '[Input Error] Password Confirm',
  msg: 'Both passwords must be the same'
}

export const HTMLTextareaError = {
  typeError: '[Textarea Error] infoToBeEncrypted',
  msg: 'Information is required and must have more than one character'
}

export const HTMLTextareaHashError = {
  typeError: '[Textarea Hash Error] infoToBeDecrypted',
  msg: 'Hash is required and must be 64 characters long'
}